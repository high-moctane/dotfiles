# dotfiles
dotfiles

```
curl -L mocdot.ml | make apt -f -
curl -L mocdot.ml | make -f - PROTOCOL=https
```

```
curl -L https://raw.githubusercontent.com/high-moctane/dotfiles/master/Makefile | \
    make -f - PROTOCOL=https DST=${HOME}/powa BRANCH=210428
```
