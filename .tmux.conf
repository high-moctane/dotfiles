# 起動時の shell を xonsh にする
set-option -g default-shell /usr/local/bin/xonsh

# 256色表示にする
set-option -g default-terminal "screen-256color"
set -ga terminal-overrides "xterm:colors=256:Tc"

# マウスを有効にする
set-option -g mouse on

