FROM debian:buster

RUN apt-get update \
    && apt-get install -y make curl

RUN curl -L mocdot.ml | make apt all -f - PROTOCOL=https NVIM_TAG=untagged-d002fb3847115e4bcbfc

WORKDIR /root

CMD ["/bin/bash"]
